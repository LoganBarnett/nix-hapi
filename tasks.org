#+title:     nix-hapi Task Backlog
#+language:  en

Tasks are worked through together; do not mark DONE without reviewing with the
user first.  Add new tasks freely; clean up only in a joint session.

* DONE Parallelism — fake API and integration tests                    :dag:test:
  :PROPERTIES:
  :STATUS:   done
  :END:

  Implemented in ~crates/fake/~.  ~FakeProvider~ accepts ~delay_ms~ via config,
  records wall-clock start/finished times per apply, and exposes ~ApplyRecord~
  with ~overlaps_with~.  ~crates/fake/tests/wave_ordering.rs~ covers wave
  membership, parallel overlap, and wave-sequencing assertions against the
  diamond DAG.

* DONE Provider library boundary                                       :arch:
  :PROPERTIES:
  :STATUS:   done
  :END:

  Implemented as the subprocess provider boundary.  Providers are now separate
  binaries registered at CLI startup via ~--provider TYPE=PATH~ flags.
  ~resolve_provider~ in ~main.rs~ is replaced with a resolver closure that
  spawns ~SubprocessProvider~ instances.  Adding a new provider requires only a
  new ~--provider~ flag; no code changes to the CLI.

* DONE Documentation and d2 diagrams                                   :docs:
  :PROPERTIES:
  :STATUS:   done
  :END:

  ~dag.rs~ has module-level doc comments explaining waves and ~dependsOn~.
  ~overview.org~ now contains:

** DONE DAG topology d2 diagram

   Named d2 source block (~dag-topology~) and named shell rendering block
   (~render-dag-topology~) in ~overview.org~.  Output: ~docs/dag-topology.svg~.
   ~pkgs.d2~ added to ~devPackages~ in ~flake.nix~.

** DONE derivedFrom lifecycle diagram

   Named d2 source block (~derived-from-lifecycle~) and named shell rendering
   block (~render-derived-from-lifecycle~) in ~overview.org~.
   Output: ~docs/derived-from-lifecycle.svg~.

   Rendering invocation in ~overview.org~ uses
   ~org-babel-goto-named-src-block~ + ~org-babel-execute-src-block~ to target
   each block by name, avoiding whole-buffer execution.

** DONE mk-helper reference in overview.org

   "mk* Helper Reference" section added to ~overview.org~ covering field value
   constructors, dependency helpers, ~mkTree~, LDAP helpers, and the provider
   naming convention.

* DONE Executor and DAG node-level refactor                       :arch:feat:
  :PROPERTIES:
  :STATUS:   done
  :END:

  ~dag.rs~ now walks the full desired-state tree to collect edges from both
  ~__nixhapi.dependsOn~ meta blocks and ~FieldValue::DerivedFrom~ inputs at
  any depth.  ~execution_waves~ and ~topological_order~ take ~&Value~ instead
  of ~&HashMap<String, Value>~.  ~executor.rs~ updated accordingly.  20 unit
  tests in ~dag::tests~ cover chains, diamonds, cycles, error cases, and both
  cycle orientations across the two edge mechanisms.

* DONE derivedFrom — runtime-computed field values                     :feat:
  :PROPERTIES:
  :STATUS:   done
  :END:

  Sometimes a field value is not known at Nix evaluation time; it can only be
  computed at reconciliation time from values that earlier parts of the tree
  produced (e.g. a user ID assigned by an HR system that a scheduling system
  needs as a foreign key).

** Agreed design

   New ~FieldValue~ variant:
   #+begin_src rust :exports code
   DerivedFrom {
     inputs: HashMap<String, String>,  // alias → absolute jq path into full tree
     expression: String,               // jq expression evaluated with . = resolved inputs
   }
   #+end_src

   - ~inputs~ maps a short alias to an absolute jq path into the full top-level
     JSON blob (e.g. ~"uid": ".[\"hr-system\"].users.alice.id"~).
   - Each ~inputs~ entry creates an implicit DAG edge: the field that owns this
     ~derivedFrom~ cannot be evaluated until every aliased path resolves.
   - ~expression~ is a jq program where ~.~ is the resolved inputs object
     ~{ alias: resolved_value, ... }~.  There is no ~$inputs~ variable — the
     resolved inputs object IS the input, so aliases are accessed as ~.uid~,
     ~.schedule_id~, etc.
   - All input paths must be absolute (from root); no relative addressing.
     The Nix layer generates these paths from attribute references so verbosity
     in the JSON is not a concern for authors.
   - The expression must return a ~FieldValue~-shaped object.  The jq context
     includes injected helpers ~mkManaged~, ~mkInitial~, etc.
   - ~derivedFrom~ fields always re-evaluate on every reconciliation (semantics
     of ~managed~, not ~initial~).
   - Providers register additional scoped jq helpers (see [[*Provider jq
     functions]]).

   Nix sugar (via ~mkDerivedFrom~ and ~mkTree~):
   #+begin_src nix :exports code
   let tree = lib.mkTree config; in
   userId = lib.mkDerivedFrom {
     inputs = { uid = tree."hr-system".users.alice.uid.__nixhapi_path; };
     expression = "mkManaged(.uid)";
   };
   #+end_src

   A good provider ~mk~ function encodes its own ~derivedFrom~ dependencies so
   callers do not need to think about ordering:
   #+begin_src nix :exports code
   # mkScheduleMembership knows that user_id and schedule_id come from
   # their respective providers and declares the inputs itself.
   membership = sched.mkScheduleMembership {
     user   = config.hr-system.users.alice;
     sched  = config.scheduling.schedules.on-call;
   };
   #+end_src

** Prerequisite: node-level DAG refactor

   The current ~dag.rs~ / ~executor.rs~ operate at the provider-instance level
   (top-level JSON keys).  ~derivedFrom~ requires a DAG built from every node
   in the tree, not just provider scopes:

   - DAG nodes are arbitrary jq paths into the full tree.
   - A ~derivedFrom~ field at path ~P~ adds edges ~(inputs[i] → P)~ for each
     alias ~i~.
   - ~dependsOn~ at the provider-instance level becomes a special case of the
     same mechanism (provider scope node depends on another provider scope node).
   - Providers are not special; they are simply at the root by convention.

   This refactor must land before ~derivedFrom~ can be implemented.  See
   [[*Executor and DAG node-level refactor]].

** Open questions

   - ~inputPath~ in Nix: resolved.  ~mkTree config~ returns a path-aware
     proxy; every node carries a ~path~ attribute containing its absolute jq
     address.  Authors write ~tree."hr-system".users.alice.uid.__nixhapi_path~.

** DONE Implement derivedFrom FieldValue variant
   ~FieldValue::DerivedFrom { inputs, expression }~ added to ~field_value.rs~.
   Calling ~resolve()~ on it returns ~DerivedFromNotResolved~; the executor
   handles evaluation between waves.  Nix helper (~mkDerivedFrom~) still TODO.

** DONE Implement mkDerivedFrom Nix helper in nix/lib/default.nix
   ~mkTree~ builds a path-aware proxy of any config attrset; every node
   carries a ~path~ attribute containing its absolute jq address.  FieldValue
   leaves (string ~__nixhapi~ discriminant) stop recursion.
   ~mkDerivedFrom { inputs, expression }~ emits the ~derived-from~ JSON shape.
   Usage: ~tree."hr-system".users.alice.uid.__nixhapi_path~ produces
   ~.[\"hr-system\"][\"users\"][\"alice\"][\"uid\"]~.

** DONE Wire derivedFrom resolution into executor (post-refactor)
   ~crates/lib/src/derived.rs~ added with ~resolve_derived_from_tree~.
   ~execute_apply_waves~ now maintains a crystalized tree; calls ~list_live~
   after each wave and resolves ~DerivedFrom~ nodes before the next wave.
   ~FakeProvider~ extended with ~with_live_state~ and ~with_plan_snapshots~.
   ~crates/fake/tests/derived_from_integration.rs~ covers single-hop, chain,
   and diamond patterns end-to-end.  48 tests pass.

* DONE derivedFrom plan-phase display                                 :feat:
  :PROPERTIES:
  :STATUS:   done
  :END:

  At plan time no apply has happened, so all ~derivedFrom~ inputs are
  unsatisfied.  Rather than omitting these fields silently, the plan diff
  should show them as a pending change with the input paths visible, e.g.:

  #+begin_example
  + userId: <derived from uid=.["hr-system"]["users"]["alice"]["uid"]>
  #+end_example

  Skip-if-not-saturated is already the apply-time behavior (like partial
  application — nothing executes until all inputs are present).  No
  distinction between partially vs fully unsatisfied is needed at plan time.

* DONE Provider jq functions                                          :feat:
  :PROPERTIES:
  :STATUS:   done
  :END:

  Providers are not aware of each other at runtime — no registration
  mechanism is needed.  Provider authors encode their domain knowledge into
  Nix ~mk*~ helpers (e.g. ~mkScheduleMembership~) that emit the correct
  ~dependsOn~ and ~derivedFrom~ constellations, using ~lib.mkTree~ to
  compute the required jq paths.  Consumers call the helper; the helper
  handles all the wiring.

  The core ~mk*~ functions (~mkManaged~, ~mkInitial~, etc.) are always
  available in every ~derivedFrom~ expression via the ~MK_HELPERS~ preamble
  in ~crates/lib/src/derived.rs~.  No further mechanism is required.

  No implementation work needed.  Close this task.

* DONE Shadow values — virtual fields for composition                  :feat:
  :PROPERTIES:
  :STATUS:   resolved-by-design
  :END:

  Resolved by design — no implementation needed.

  Values outside any provider scope are already inert: no provider sees or
  diffs them.  The ~derivedFrom~ mechanism already covers the composition
  use case.  The original intra-provider intermediate value scenario (a
  field inside a provider scope that should not be diffed) is deferred
  until a concrete provider actually needs it.

* DONE tasks.org cleanup                                               :meta:
  :PROPERTIES:
  :STATUS:   done
  :END:

* TODO split_scope silently swallows malformed __nixhapi metadata  :bug:core:
  :PROPERTIES:
  :ID:       1.1
  :END:

  ~executor.rs:64-68~: ~.ok()~ on ~serde_json::from_value~ means a malformed
  ~__nixhapi~ object (e.g. ~"provider": 42~) is silently treated as having no
  metadata.  Same pattern in ~dag.rs:258-259~ (~walk_for_edges~).

  - Test first: construct a scope with invalid ~__nixhapi~ shape, assert error.
  - Fix: add ~ExecuteError::MetadataParse { instance, message }~, propagate.
  - Fix ~dag.rs~: add ~DagError::MetadataParse { instance, message }~.

* TODO plan_instance_inner panics on missing instance              :bug:core:
  :PROPERTIES:
  :ID:       1.3
  :END:

  ~executor.rs:97~: ~root.get(instance_name).expect("instance must exist")~
  panics defensively.

  - Fix: replace ~.expect(...)~ with ~.ok_or_else(|| ExecuteError::MissingInstance { ... })?~.
  - Add ~MissingInstance { instance: String }~ to ~ExecuteError~.

* TODO Subprocess EOF produces confusing error                     :bug:core:
  :PROPERTIES:
  :ID:       1.6
  :END:

  ~subprocess.rs:99-106~: when a provider binary crashes, ~read_line~ returns
  an empty string, parsed by ~serde_json::from_str("")~ as a confusing JSON
  error.

  - Test: spawn with ~/usr/bin/true~, assert EOF-specific error message.
  - Fix: check for empty line, return ~SubprocessError::UnexpectedEof~.

* TODO dependsOn value-equality matching is fragile               :bug:core:
  :PROPERTIES:
  :ID:       1.2
  :END:

  ~dag.rs:262-269~: resolution evaluates jq, then searches every top-level
  value for equality.  Fails with duplicate scope content; O(n) per dep.

  - Test: three instances where ~a~ and ~b~ have identical scope content,
    ~c~ depends on ~.["a"]~.  Assert resolved dependency is ~"a"~ not ~"b"~.
  - Fix: extract key from expression via ~instance_from_input_path~ and verify
    the instance exists.  Fall back to eval for arbitrary jq.  Warn on
    ambiguity.

* TODO execute_apply_waves discards reports on partial failure     :bug:core:
  :PROPERTIES:
  :ID:       1.4
  :END:

  ~executor.rs:240-252~: when one instance in a wave fails, ~result?~ discards
  successful reports.

  - Test: two-instance wave, one provider errors.  Assert successful report is
    still accessible.
  - Fix: collect all results, partition successes/failures.  Push successes
    into reports; return ~ExecuteError::PartialWaveFailure~ wrapping both.

* TODO Subprocess wire protocol loses Initial/Managed semantics    :bug:core:
  :PROPERTIES:
  :ID:       1.5
  :END:

  ~subprocess.rs:133-138~ and ~provider_host.rs:121-134~: ~to_wire_config~
  flattens to plain strings; ~parse_config~ reconstructs all as ~Managed~.

  - Test: pass ~Initial~ config, assert provider side retains ~Initial~.
  - Fix: wire format carries ~{tag, value}~ objects.  ~DerivedFrom~ config
    fields produce an error.

* TODO Executor test coverage                                     :test:core:
  :PROPERTIES:
  :ID:       1.7
  :END:

  Zero unit or integration tests for ~execute_plan_waves~ / ~execute_apply_waves~.

  - Single instance, empty plan → no apply.
  - Single instance, non-empty plan → apply called, report returned.
  - Two-wave diamond → wave ordering respected.
  - Missing ~__nixhapi.provider~ → ~MissingProvider~ error.
  - Provider that returns error from ~plan~ → ~ProviderOperation~ error.
  - Provider that returns error from ~apply~ → ~ProviderOperation~ error.

* TODO DerivedFrom plan-time saturation check                    :feat:core:
  :PROPERTIES:
  :ID:       2.1
  :END:

  At plan time, validate that every ~DerivedFrom~ node's inputs will be
  satisfied by the DAG ordering.

  - New file: ~crates/lib/src/saturation.rs~.
  - ~check_derived_from_saturation(root, waves) -> Result<(), SaturationError>~.
  - Call from ~execute_plan_waves~ after computing waves.
  - Tests: valid diamond, same-wave reference (error), missing instance (error),
    self-reference (ok).

* TODO mkTree path key collision                                   :bug:nix:
  :PROPERTIES:
  :ID:       4.1
  :END:

  ~nix/lib/default.nix:85~: ~mkTree~ adds ~path~ via ~// { path = prefix; }~.
  Shadows any user data key named ~path~.

  - Fix: rename to ~__nixhapi_path~.
  - Update ~mkDerivedFrom~ examples and docs.

* TODO JqExpr type — inline string or file reference             :feat:core:
  :PROPERTIES:
  :ID:       6.2
  :END:

  New type in ~crates/lib/src/jq_expr.rs~: ~JqExpr~ enum with ~Inline~,
  ~Structured~ variants (~jq-expr~, ~jq-file~).  Plain JSON string
  deserializes as ~JqExpr::Inline~.

  - Update ~NixHapiMeta.depends_on~ from ~Vec<String>~ to ~Vec<JqExpr>~.
  - Update ~NixHapiMeta.ignore~ to ~Vec<JqExpr>~.
  - Add Nix helpers: ~mkJqExpr~, ~mkJqFile~.

* TODO jq-based ignores replacing regex                          :feat:core:
  :PROPERTIES:
  :ID:       6.1
  :BLOCKED_BY: 6.2
  :END:

  Replace regex ignore patterns with jq filter expressions evaluated against
  live-state tree.  Core reconciler evaluates ignores; providers never see them.

  - Remove ~compile_ignore_patterns~ from LDAP provider.
  - Move ignore evaluation to executor/core.
  - Migration: regex patterns become jq expressions with ~test()~.

* TODO NixOS/nix-darwin module for host-specific trees             :feat:nix:
  :PROPERTIES:
  :ID:       3.1
  :END:

  ~nix/modules/default.nix~: ~services.nix-hapi~ with per-tree options
  (~desiredState~, ~schedule~, ~providers~).  Generate systemd services/timers.

* TODO Flake-level nix-hapi-trees for non-host trees               :feat:nix:
  :PROPERTIES:
  :ID:       3.2
  :END:

  ~mkFlakeTrees~ helper in ~nix/lib/default.nix~.  Consumers declare trees in
  flake outputs, run from CI or non-NixOS hosts.

* TODO Wire up flake outputs for modules and lib                   :feat:nix:
  :PROPERTIES:
  :ID:       3.3
  :END:

  Export ~nixosModules.default~ and updated ~lib~ from ~flake.nix~.
